# スタンプ生成機能 改良実装タスク一覧（ToDo分解版）

## 0. この改良の位置づけ

本対応は、前回仕様を作り直すものではなく、**既存実装をベースにした差分改良**とする。

### 今回の新規追加
- GPT向け「5シート同時生成」モード
- GPT向け「1シートずつ生成 / 5シート同時生成」の切り替え
- スタンプ全体の仕上がりスタイル設定
- 複数シート生成時のシリーズ統一性を意識したプロンプト生成
- 5シート同時生成結果の個別管理・表示

### 既存仕様に含まれており、今回も実装対象として継続するもの
- 背景透過処理
- 透過PNG出力
- 3×3グリッド
- 1シート9スタンプ
- キャラクター参照画像の利用
- セリフ・表情・ポーズ・向きの指定
- 個別シート生成
- 保存 / ダウンロード等の既存機能

> **重要:** 背景透過は今回の追加仕様ではない。  
> 前回仕様ですでに要求済みのため、未実装であれば「既存仕様の残タスク」として実装する。

---

# 1. 既存実装確認

## 1.1 現状コードの確認
- [ ] 現在のスタンプ生成画面の構成を確認する
- [ ] 既存の生成フォームの入力項目を確認する
- [ ] 現在の1シート生成フローを確認する
- [ ] プロンプト生成処理の実装箇所を確認する
- [ ] モデル選択処理の実装箇所を確認する
- [ ] 生成API呼び出し処理を確認する
- [ ] 生成結果の保存形式を確認する
- [ ] 生成結果一覧のデータ構造を確認する
- [ ] 背景透過処理の実装状況を確認する
- [ ] 透過PNG保存処理の実装状況を確認する
- [ ] 既存仕様と現コードの差分を整理する

## 1.2 既存機能を壊さないこと
- [ ] 現在の1シート生成をそのまま利用できる状態を維持する
- [ ] 既存の入力データ形式との互換性を維持する
- [ ] 既存保存データがある場合は読み込み互換性を確認する
- [ ] 既存生成履歴がある場合は表示互換性を確認する

---

# 2. 生成モード追加

## 2.1 GPT生成モード

GPTを選択した場合、以下の2モードを選択可能にする。

### モードA: 1シートずつ生成
- [ ] 既存の1回1シート生成を維持する
- [ ] 1シート = 3×3 / 9スタンプとする
- [ ] 既存UI・既存処理をできるだけ再利用する

### モードB: 5シート同時生成
- [ ] 新規モードとして追加する
- [ ] 1回の操作で5シート分を生成要求できるようにする
- [ ] 1シート9スタンプ × 5シート = 45スタンプを扱えるようにする
- [ ] **45スタンプを1枚の巨大画像にまとめない**
- [ ] **3×3の独立した正方形シートを5枚出力させる**
- [ ] 5枚を同一シリーズとして生成するようプロンプトを構築する
- [ ] 5枚それぞれを独立した生成結果として管理する

## 2.2 モード選択UI
- [ ] GPT選択時のみ生成方式を表示する
- [ ] 「1枚ずつ生成」
- [ ] 「5枚まとめて生成」
- [ ] 初期値を決定する
- [ ] 選択内容を生成ジョブに保存する

### 推奨初期値
- 既存ユーザーへの影響を避ける場合: `1枚ずつ生成`
- 新規UIでシリーズ生成を強く推す場合: 要検討

---

# 3. 5シート入力UI

## 3.1 シート管理
- [ ] シート1〜シート5を編集可能にする
- [ ] 各シートに9件のスタンプ指定を持たせる
- [ ] 45件を1画面に単純縦並びしない
- [ ] タブ / アコーディオン / ページ切替等で扱いやすくする
- [ ] 現在編集中のシート番号を明示する

## 3.2 各スタンプの入力項目
既存仕様を継続する。

- [ ] セリフ
- [ ] 表情・感情
- [ ] ポーズ・動作
- [ ] 見せ方 / 向き
- [ ] 必要であれば既存の追加指定欄

## 3.3 入力検証
- [ ] 1シート9件そろっているか確認する
- [ ] 5枚同時生成時は5シート分の入力状態を確認する
- [ ] 空欄の扱いを既存仕様に合わせる
- [ ] 長すぎるセリフへの既存制限があれば維持する

---

# 4. 汎用キャラクター参照仕様

## 4.1 キャラクター固定
- [ ] 添付された参照キャラクターを全シートで共通利用する
- [ ] 5枚すべてで同一キャラクターとして維持するよう指示する
- [ ] 参照画像の外見、衣装、体型、質感等を維持するよう指示する

## 4.2 テンプレートをキャラクター固有にしない
以下のような固有特徴を共通テンプレートへ直書きしない。

- [ ] 特定の色
- [ ] 特定の衣装
- [ ] 特定のアクセサリー
- [ ] 特定の動物種
- [ ] 特定の頭身
- [ ] 特定の3D / 2D画風

### NG例
`黄色い毛並み、青い水玉スカーフ、茶色いリュックを維持する`

### OK例
`添付した参照画像で確認できるキャラクターの外見、衣装、体型、質感、アクセサリー等を維持する`

---

# 5. プロンプトビルダー改修

## 5.1 共通プロンプト
モデルへ過剰なスタイル指定を行わず、必要条件を中心に構成する。

- [ ] 参照キャラクターを使用
- [ ] 同一シリーズとして統一
- [ ] 1シート3×3
- [ ] 1シート9スタンプ
- [ ] 各スタンプにキャラクター1体
- [ ] 各スタンプに指定セリフ1つ
- [ ] 各スタンプを独立配置
- [ ] 隣接スタンプの接触 / 重なり禁止
- [ ] スタンプ間に十分な空間を確保
- [ ] 正方形画像
- [ ] 背景透過を要求
- [ ] 文字は正確で読みやすくする

## 5.2 不要な固定指定を避ける
標準プロンプトでは以下を強制しない。

- [ ] 「文字をカラフルにする」を固定しない
- [ ] 「丸みのある太字」を固定しない
- [ ] 装飾量を固定しない
- [ ] 星 / ハート / 汗 / 効果線等を細かく制御しない
- [ ] 特定の文字色を固定しない
- [ ] キャラ固有のデザイン説明を入れない

> GPT側のステッカー表現が自然に働く余地を残す。

---

# 6. GPT 5シート同時生成プロンプト

## 6.1 必須指示
5枚同時生成時は、以下を強く明示する。

- [ ] シート1〜5をそれぞれ独立した正方形画像として生成する
- [ ] 合計5枚の画像を生成する
- [ ] 5シートを1枚のコラージュにまとめない
- [ ] 各画像は3×3 / 9スタンプ
- [ ] 5枚すべてを同じシリーズとして統一する
- [ ] 同じ参照キャラクターを使用する
- [ ] 同一のステッカー表現・質感を可能な範囲で維持する

## 6.2 シート内容の分離
- [ ] シート1の9件
- [ ] シート2の9件
- [ ] シート3の9件
- [ ] シート4の9件
- [ ] シート5の9件

を明確な見出しで分離してプロンプトへ渡す。

## 6.3 誤生成対策
- [ ] `45種類を1枚の画像にまとめない` を末尾でも再確認する
- [ ] `5 separate square sticker sheets` 相当の意味をモデル別に最適化する
- [ ] 返却画像数をチェックする
- [ ] 1枚しか返らなかった場合の扱いを実装する

---

# 7. 仕上がりスタイル設定

## 7.1 全モデル共通設定として追加
FLOW専用、GPT専用にはしない。

### 候補
- [ ] おまかせ
- [ ] キャラクター重視
- [ ] バランス
- [ ] 文字くっきり
- [ ] 落ち着いた仕上がり

## 7.2 初期値
- [ ] `おまかせ` を初期値候補とする

理由:
- モデル本来のステッカー表現を活かせる
- 不要な制約を減らせる
- 今回の検証で自然な装飾表現が有効だった

## 7.3 内部プロンプト差分

### おまかせ
- [ ] 最低限の共通条件のみ追加
- [ ] 文字色・装飾・レイアウトの細部はモデルに任せる

### キャラクター重視
- [ ] キャラクターを主役として見せる
- [ ] 文字がキャラクターを圧迫しないよう指示する

### バランス
- [ ] キャラクターとセリフ双方の視認性を確保する

### 文字くっきり
- [ ] セリフを見やすく目立たせる
- [ ] 文字の視認性を優先する
- [ ] 必要に応じて文字表現を強める
- [ ] 「カラフル」はこのプリセットの調整候補とするが、実機検証後に確定する

### 落ち着いた仕上がり
- [ ] ビジネス用途でも使いやすい方向へ寄せる
- [ ] 文字や全体表現を過度に派手にしない

---

# 8. スタイルプリセット検証

仕上がりスタイルはモデルごとの反応差が大きいため、プロンプトだけで期待通り制御できるか確認する。

## 8.1 テスト
同一キャラクター・同一9セリフで比較する。

- [ ] おまかせ
- [ ] キャラクター重視
- [ ] バランス
- [ ] 文字くっきり
- [ ] 落ち着いた仕上がり

## 8.2 確認項目
- [ ] キャラクターサイズ
- [ ] 文字サイズ
- [ ] 文字色の傾向
- [ ] 文字の占有面積
- [ ] 装飾の出方
- [ ] ビジネス向けセリフの見え方
- [ ] シリーズとしての統一感

## 8.3 判定
- [ ] 見た目の差が十分出るプリセットだけ正式採用する
- [ ] 差が曖昧なプリセットは削除 / 統合する
- [ ] UIだけ増やして実質変化しない設定は実装しない

---

# 9. 背景透過処理（既存仕様の実装 / 継続）

> **新規仕様ではない。前回仕様に含まれる既存要件。**

## 9.1 実装状況確認
- [ ] 現在透過処理が未実装か確認する
- [ ] 一部実装済みの場合は完成度を確認する
- [ ] GPT生成結果が真のアルファ透過か確認する
- [ ] 疑似透過画像の判定方法を確認する

## 9.2 モデル別処理

### 真の透明背景を出力できるモデル
- [ ] ネイティブ透過を優先する
- [ ] アルファチャンネルを検証する
- [ ] 問題なければそのままPNG出力する

### 背景透過できない / 疑似透過になるモデル
- [ ] 後処理で背景除去する
- [ ] PNGアルファとして保存する
- [ ] チェッカーボード模様自体が画像に描かれている場合も除去対象として扱えるか検討する

## 9.3 品質要件
- [ ] キャラクターを削らない
- [ ] 文字を削らない
- [ ] 白いステッカー縁を削らない
- [ ] 必要な小物を削らない
- [ ] エッジのギザつきを抑える
- [ ] キャラクター内部の白色部分等を誤透過しない

## 9.4 元画像保持
- [ ] 透過前画像を保持する
- [ ] 透過失敗時に元画像へ戻せるようにする

---

# 10. 5シート生成結果の管理

## 10.1 データ構造
1つの生成ジョブに複数シートを紐付ける。

```text
GenerationJob
 ├─ Sheet 1
 ├─ Sheet 2
 ├─ Sheet 3
 ├─ Sheet 4
 └─ Sheet 5
```

- [ ] ジョブID
- [ ] 生成モデル
- [ ] 生成モード
- [ ] 仕上がりスタイル
- [ ] シート数
- [ ] 共通参照画像
- [ ] 各シートの入力情報
- [ ] 各シートの生成状態
- [ ] 各シートの生成画像
- [ ] 各シートの透過済み画像

---

# 11. 生成ステータス

## 11.1 ジョブ単位
候補:

- [ ] waiting
- [ ] generating
- [ ] post_processing
- [ ] completed
- [ ] partial_failed
- [ ] failed

## 11.2 シート単位
- [ ] waiting
- [ ] generating
- [ ] generated
- [ ] transparent_processing
- [ ] completed
- [ ] failed

## 11.3 部分成功
5枚中4枚成功などを許容する。

- [ ] ジョブ全体を即failedにしない
- [ ] 成功したシートはそのまま利用可能にする
- [ ] 失敗したシートのみ再試行可能にする

---

# 12. 結果表示UI

## 12.1 1シート生成
- [ ] 現行表示を維持する

## 12.2 5シート生成
- [ ] 5枚を個別カードとして表示する
- [ ] シート番号を表示する
- [ ] 各シートの状態を表示する
- [ ] 完了したものから順次表示できる構造を検討する

## 12.3 シートごとの操作
- [ ] プレビュー
- [ ] ダウンロード
- [ ] 個別再生成
- [ ] 透過再処理
- [ ] 削除
- [ ] 既存機能に保存操作がある場合は統合する

---

# 13. 再生成仕様

## 13.1 1枚ずつ生成モード
- [ ] 現行の再生成仕様を維持する

## 13.2 5枚同時生成モード
- [ ] 全5枚を再生成
- [ ] 失敗したシートだけ再生成
- [ ] 任意の1シートだけ再生成

### 最低要件
- [ ] 任意の1シートだけ再生成可能

---

# 14. 5枚同時生成とシリーズ統一

今回の検証では、**同一リクエスト内で5枚まとめて生成した場合、1枚ずつ別々に生成するよりシリーズ感が揃いやすい傾向が確認された。**

- [ ] 5枚モードでは同一リクエスト内でまとめて要求する
- [ ] 5枚すべてに同一の共通スタイル条件を適用する
- [ ] シートごとに独立画像として返すよう要求する
- [ ] キャラクター参照を5枚共通にする

> この傾向はモデルの生成挙動によるもので、完全一致を保証する仕様にはしない。

---

# 15. モデル別アダプター

UI側の設定名は共通とし、モデル別の違いは内部で吸収する。

```text
UI
 ↓
Generation Settings
 ↓
Prompt Builder
 ↓
Model Adapter
 ├─ GPT
 ├─ NanoBanana / Gemini系
 └─ その他
```

- [ ] モデル固有プロンプトをUIへ露出しない
- [ ] `style_preset` をモデル別プロンプトへ変換する
- [ ] `generation_mode` の対応可否をモデルごとに管理する
- [ ] 5枚同時生成非対応モデルではUI選択不可にする

---

# 16. 推奨データ項目

## GenerationJob

```text
id
model
generation_mode
style_preset
sheet_count
status
reference_image
created_at
updated_at
```

## Sheet

```text
id
job_id
sheet_index
status
source_image
transparent_image
prompt_snapshot
error
```

## StampItem

```text
id
sheet_id
item_index
text
expression
pose
view
```

既存DB構造がある場合は、無理にこの形へ作り直さず、必要な差分だけ追加する。

---

# 17. API / サーバー側タスク

- [ ] 生成リクエストに `generation_mode` を追加
- [ ] 生成リクエストに `style_preset` を追加
- [ ] 5シート分の入力を受け取れるようにする
- [ ] GPT 5シート同時生成用プロンプトを組み立てる
- [ ] 複数画像レスポンスを処理する
- [ ] 返却画像をシート番号へ紐付ける
- [ ] 返却枚数を検証する
- [ ] 各画像へ既存の透過処理パイプラインを適用する
- [ ] 部分成功を扱えるようにする
- [ ] シート単位再生成APIを追加 / 拡張する

---

# 18. フロントエンドタスク

- [ ] GPT生成モード選択UI追加
- [ ] 仕上がりスタイル選択UI追加
- [ ] 5シート編集UI追加
- [ ] シートタブ / アコーディオン等の操作実装
- [ ] 5枚生成の進捗表示
- [ ] 5枚結果表示
- [ ] シート単位エラー表示
- [ ] シート単位再生成
- [ ] シート単位ダウンロード
- [ ] 透過済み画像の表示
- [ ] 既存モバイルUIを崩さない

---

# 19. エラー処理

## 19.1 生成画像数不足
例: 5枚要求して3枚しか返らない。

- [ ] 成功3枚を保持する
- [ ] 不足2枚をエラー状態にする
- [ ] 不足分のみ再生成できるようにする

## 19.2 1枚コラージュで返された場合
- [ ] 正常な5枚出力として扱わない
- [ ] 再生成対象とする
- [ ] 自動再試行を行う場合は回数上限を設定する

## 19.3 透過失敗
- [ ] 生成元画像は保持する
- [ ] 透過処理だけ再実行可能にする

## 19.4 セリフ誤り
完全自動判定が難しい場合:
- [ ] ユーザーが対象シートを再生成できるようにする
- [ ] 将来的なOCR / Vision確認は別タスクとする

---

# 20. テスト

## 20.1 既存回帰テスト
- [ ] 1シート生成が従来通り動く
- [ ] 既存保存が動く
- [ ] 既存ダウンロードが動く
- [ ] 既存編集が動く
- [ ] モバイル操作が壊れていない

## 20.2 GPT 5シート生成
- [ ] 5枚要求 → 5枚返却
- [ ] 各画像3×3
- [ ] 各シート9個
- [ ] 1枚コラージュになっていない
- [ ] キャラクターが全5枚で同一シリーズに見える
- [ ] シート内容が混ざっていない

## 20.3 透過
- [ ] GPTネイティブ透過
- [ ] 疑似透過画像
- [ ] 白背景
- [ ] チェッカーボード風背景
- [ ] 白いキャラクターパーツを含む画像
- [ ] 白い文字フチを含む画像

## 20.4 スタイルプリセット
- [ ] おまかせ
- [ ] キャラクター重視
- [ ] バランス
- [ ] 文字くっきり
- [ ] 落ち着いた仕上がり

---

# 21. 受け入れ条件

## 必須
- [ ] 既存1シート生成が壊れていない
- [ ] GPTで1シートずつ生成できる
- [ ] GPTで5シートを1回の操作で生成できる
- [ ] 5シートは5枚の独立画像として出力される
- [ ] 5枚が同じシリーズとして成立する
- [ ] 各シートが3×3 / 9スタンプ
- [ ] 生成結果をシート単位で扱える
- [ ] シート単位で再生成できる
- [ ] 前回仕様の背景透過処理が実装されている
- [ ] 最終成果物を透過PNGとして取得できる

## スタイル設定
- [ ] 仕上がりスタイルを選択できる
- [ ] `おまかせ` が利用できる
- [ ] 少なくとも「キャラクター重視」と「文字くっきり」の違いが目視可能
- [ ] 効果が薄いプリセットは正式リリース前に整理する

---

# 22. 実装優先順位

## Phase 1: 既存仕様残タスク確認
- [ ] 既存コード確認
- [ ] 背景透過の実装状況確認
- [ ] 未実装なら前回仕様どおり完成させる

## Phase 2: GPT 5シート同時生成
- [ ] generation_mode追加
- [ ] 5シート入力
- [ ] 5シートプロンプト生成
- [ ] 複数画像受信
- [ ] 5枚結果表示
- [ ] シート単位管理
- [ ] 個別再生成

## Phase 3: 仕上がりスタイル
- [ ] style_preset追加
- [ ] おまかせ実装
- [ ] 各プリセットのプロンプト差分作成
- [ ] モデル別検証
- [ ] UI追加

## Phase 4: 品質改善
- [ ] 透過品質改善
- [ ] エラー復旧改善
- [ ] モデル別プロンプト最適化
- [ ] 必要に応じて自動品質チェック

---

# 23. 実装時の重要事項

1. **前回仕様を捨てて作り直さない**
2. **背景透過は今回の新規追加ではなく、前回仕様の既存要件**
3. **GPT 5枚同時生成が今回の主要追加機能**
4. **5枚同時生成 = 45個を1枚にまとめることではない**
5. **3×3シートを5枚、独立画像として生成する**
6. **汎用テンプレートへ特定キャラクターの特徴を直書きしない**
7. **標準状態では文字色や装飾を過剰に制御しない**
8. **スタイル指定は全モデル共通UIとして扱う**
9. **モデルごとの差は内部のPrompt Builder / Adapterで吸収する**
10. **5枚中一部失敗でも成功画像を捨てない**
11. **5枚まとめ生成のシリーズ統一性を活かす**
12. **既存の1枚ずつ生成モードを必ず残す**

---

# 24. 完了イメージ

```text
キャラクター画像を選択
       ↓
モデルを選択
       ↓
GPTの場合
「1枚ずつ」or「5枚まとめて」を選択
       ↓
必要なら仕上がりスタイルを選択
       ↓
セリフ / ポーズを入力
       ↓
生成
       ↓
3×3ステッカーシートを取得
       ↓
既存仕様の背景透過処理
       ↓
透過PNGとして保存 / ダウンロード
```

5枚まとめて生成する場合:

```text
1回の生成操作
       ↓
Sheet 1 ─ 9スタンプ
Sheet 2 ─ 9スタンプ
Sheet 3 ─ 9スタンプ
Sheet 4 ─ 9スタンプ
Sheet 5 ─ 9スタンプ
       ↓
同一シリーズの5枚
       ↓
各シートを個別に保存 / 再生成可能
```
