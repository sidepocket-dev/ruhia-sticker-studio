# TEST_PLAN.md

RUHiA Sticker Studio のテスト計画。テストケースと受け入れ基準はこの文書を単一の真実とする。

---

## 1. テストの三層構造

| 層 | ツール | 環境 | 対象 | 目安速度 |
|---|---|---|---|---|
| 単体 | Vitest | node | `src/core/**` の純粋関数 | 1秒未満 |
| 画像回帰 | Vitest + PNGデコーダ | node | 実AI生成シートに対する検出結果 | 数秒 |
| E2E | Playwright | 実ブラウザ | 全フロー・ZIP内容・復元・オフライン | 数十秒 |

`src/core/**` を DOM 非依存の純粋関数に保つ（PRODUCT_SPEC §77.3）ため、画像アルゴリズムのテストに
ブラウザも jsdom も node-canvas も必要ない。これがテスト戦略全体の前提。

---

## 2. コマンド

```bash
npm test            # 単体 + 画像回帰
npm run test:e2e    # E2E
npm run typecheck   # 型チェック
npm run check       # typecheck + test
```

---

## 3. フィクスチャ

### 3.1 合成フィクスチャ（単体テストの土台）

`tests/helpers/make-sheet.ts` で、指定した矩形群を描いたアルファマスクをコード生成する。
実画像に依存せず、境界条件を意図的に作れる。

| 名前 | 内容 | 目的 |
|---|---|---|
| `even-3x3` | 完全に均等な3×3、広い透明の隙間 | 基本動作 |
| `shifted-gutter` | 谷が中心から8%ずれた3×3 | 谷探索の妥当性 |
| `text-overflow` | 文字がセル境界を10px越える | 単純分割の切断検出 |
| `detached-heart` | ハートが本体から40px離れている | 近接グルーピング |
| `faint-shadow` | 全面に alpha=10 の薄い影 | 閾値・面積フィルタ |
| `white-outline` | 白フチ付きダイカット | 白フチが欠けないこと |
| `extreme-aspect` | 極端に縦長／横長の内容 | リサイズ計算 |

### 3.2 実画像フィクスチャ

`tests/fixtures/` に実際のAI生成シートを置き、期待値を JSON で固定する。
名前の付け方は2種類ある。

- **`sheet-<英字>.png`** — 特徴の違いを押さえるためのシート。1枚ごとに独立した検証用
- **`sheet-<数字>.png`** — このツールの計画から作った、実際の40個セット用のシート

#### 特徴別のシート

| ID | ファイル | 内容 | 採用される方式 |
|---|---|---|---|
| A | `sheet-a.png` | 整列した透過3×3。装飾（効果線・しずく）が本体から離れている | 単純分割 |
| B | `sheet-b.png` | 白フチ付きの自由配置。行ごとに隙間の位置が異なる | まとめ上げ |
| C | `sheet-c.png` | 文字が多いシート | 単純分割 |
| D | `sheet-d.png` | 白フチ付き・装飾が離れているシート | まとめ上げ |
| E | `sheet-e.png` | 白フチなし・装飾が多いシート | 単純分割 |
| F | `sheet-f.png` | 1024px の小さめのシート（他は1254px） | 単純分割 |

いずれも9個抽出・取りこぼし0%・重なり0px・確信度1.00。
文字と装飾が正しいキャラクターに付いていることを目視で確認済み。

#### 計画から作ったシート

| ID | ファイル | 内容 |
|---|---|---|
| 1 | `sheet-1.png` | 日常用カジュアルの1周目9件から生成した1枚目 |

このツールが組み立てた計画を、そのまま画像生成プロンプトにして作ったシート。

```text
日常用・カジュアル → 1周目の9件 → 画像生成プロンプト → 生成 → 抽出
```

**計画 → プロンプト → 生成 → 抽出 の全経路が通ることを確かめられる唯一の例**なので、
セリフの並びと抽出の並びが対応していることを固定している。
2枚目以降がそろえば、40個セット全体を実物で検証できる。

#### 検証しやすくするための追加フィクスチャ

| ファイル | 内容 | 用途 |
|---|---|---|
| `opaque.png` | 背景が透明でない画像 | 読み込みを断る動作の確認 |
| `chatgpt-answer-business.txt` | 実際のChatGPTの回答（重複だらけ） | 重複検出とパーサの回帰テスト |

期待値ファイル：`tests/fixtures/sheet-<id>.expected.json`（採用した方式と9個の矩形）

**合格基準：** 各矩形の **IoU ≧ 0.9**

期待値は初回に人間が目視確認して確定し、以降は回帰テストとして固定する。
アルゴリズム変更で期待値を更新する場合は、更新理由をコミットメッセージに書く。

## 4. 最重要の自動不変条件

抽出結果の正しさを、目視ではなく次の2式で機械的に判定する。

```text
[C1] 内容欠損なし
     抽出9枚の不透明画素の合計 ÷ 元シートの不透明画素 ≧ 0.995

[C2] 二重取得なし
     各グループ矩形どうしの重なり面積 = 0
```

C1 は「内容を切っていない」を、C2 は「同じ内容を2枚に入れていない」を検出する。
全フィクスチャ（合成・実画像とも）に対して常時適用する。

---

## 5. テストケース（原仕様§62 の TC01–TC15）

| ID | 内容 | 層 | 合格基準 | 導入フェーズ |
|---|---|---|---|---|
| TC01 | 整列シート → 9個抽出 | 画像回帰 | Fixture A で9個、IoU ≧ 0.9 | Phase 1 |
| TC02 | 内容が切れていない | 単体 + 画像回帰 | 不変条件 C1 / C2 | Phase 1 |
| TC03 | 透明余白がトリムされている | 単体 | 出力矩形の外周1pxに不透明画素が存在する（余白 = 安全余白のみ） | Phase 1 |
| TC04 | 8個選択 → 8枚のPNG | E2E | ZIP内に `01.png`〜`08.png` が存在 | Phase 2 |
| TC05 | main画像 240 × 240 | E2E | `main.png` が正確に 240 × 240 | Phase 2 |
| TC06 | tab画像 96 × 74 | E2E | `tab.png` が正確に 96 × 74 | Phase 2 |
| TC07 | ZIPが生成される | E2E | `LINE_UPLOAD.zip` が保存され、展開できる | Phase 2 |
| TC08 | 複数シートで40個 | E2E | 5シート読み込み → 45候補 → 40選択 → ZIP | Phase 5 |
| TC09 | 自由配置シートの自動認識 | 画像回帰 | Fixture B で9個、IoU ≧ 0.9 | Phase 3 |
| TC10 | 離れた文字が正しくグループ化 | 画像回帰 | Fixture C / D で不変条件 C1 / C2 | Phase 3 |
| TC11 | 日本語45行の解析 | 単体 | 45件すべてが text と action に分解される | Phase 6 |
| TC12 | 書式ゆれの解析 | 単体 | §6 の全組み合わせが解析できる | Phase 6 |
| TC13 | 重複検出 | 単体 | 完全一致・正規化一致を検出し警告 | Phase 6 |
| TC14 | リロードでプロジェクト復元 | E2E | 再読込後に選択・並び順・シートが復元 | Phase 7 |
| TC15 | オフライン動作 | E2E | `file://` で全フローが通る（保存はフォールバック） | Phase 7 |

---

## 6. パーサのテスト（TC11 / TC12 詳細）

テーブル駆動で、次の直積を検証する。

**番号形式：** `1.` / `1)` / `1 ` / `①` / `１.`（全角） / `01.`
**区切り：** `｜` / `|` / `-` / `ー` / `：` / `:` / 空白
**混入要素：** Markdown見出し（`### グループ1：あいさつ`）、空行、箇条書き記号、前後の説明文

**分岐の検証：**

| 状況 | 期待されるUI表示 |
|---|---|
| 45件ちょうど | 45件読み込みました |
| 42件 | 42件読み込みました。あと3件必要です。 |
| 47件 | 47件あります。使用する45件を選んでください。 |
| 3行だけ解析失敗 | 該当行のみ表示して個別修正できる |
| action が無い行 | text のみで受理し、action は空 |

---

## 7. LINE書き出しの検証（TC04–TC08 詳細）

E2Eテストで生成された ZIP を実際に展開し、各PNGについて次を検証する。

```text
✓ PNGシグネチャが正しい
✓ カラータイプ = 6（8bit RGBA）
✓ 幅・高さがともに偶数
✓ スタンプ画像が 370 × 320 以内
✓ main.png が 240 × 240
✓ tab.png が 96 × 74
✓ 各ファイル ≦ 1,000,000 bytes
✓ ZIP全体 ≦ 60,000,000 bytes
✓ ZIP内のエントリが main.png / tab.png / NN.png のみ（余計なファイルなし）
✓ アルファチャンネルに透明画素が存在する（背景が不透明でない）
```

---

## 8. アーキテクチャ検証

`tests/unit/architecture.test.ts` で、`src/core/**` の全ファイルの import 文を静的に走査し、
`platform/` `ui/` `state/` への依存が無いことを検証する（PRODUCT_SPEC §77.3）。

同じテストで、LINE仕様の数値（`370` `320` `240` `96` `74` 等）が
`src/config/line-spec.ts` 以外にリテラルとして現れないことも検証する（原仕様§6）。

---

## 9. UX文言の検証

`tests/unit/vocabulary.test.ts` で、UIコンポーネントの文字列リテラルに
禁止語（Alpha / Connected Components / Bounding Box / Flood Fill / Blob / Cluster 等）が
含まれないことを検証する（原仕様§4）。

---

## 10. フェーズ別の受け入れ基準

| Phase | 受け入れ基準 |
|---|---|
| 0 | `npm run check` と `npm run build` が通る。docs 4本が存在 |
| 1 | TC01 / TC02 / TC03 合格。Fixture A で不変条件を満たす |
| 2 | TC04–TC07 合格。**MVP完成**（PRODUCT_SPEC §77.12 の12項目すべて） |
| 3 | TC09 / TC10 合格。Fixture B–E で不変条件を満たす |
| 4 | 検出失敗ケースを手動修正で3分以内に是正できる |
| 5 | TC08 合格 |
| 6 | TC11–TC13 合格 |
| 7 | TC14 / TC15 合格 |
| 8 | 初見ユーザー1名がURLのみでZIP到達。詰まった箇所をUI改善候補として記録 |

---

## 11. 手動確認（自動化しないもの）

自動テストで代替できないものは、フェーズ完了時にチェックリストで確認する。

- [ ] 抽出結果が目視で正しい（実画像フィクスチャ以外の新しいシートで）
- [ ] モバイル実機（iOS Safari / Android Chrome）で並び替えができる
- [ ] tab画像（96 × 74）の内容が実サイズで判別できる
- [ ] エラーメッセージが技術用語なしで理解できる
- [ ] 40個フローが現実的な時間で完了する（目標：画像生成後10分程度）
