# AGENTS.md

RUHiA Sticker Studio のリポジトリで作業するAIエージェント（Claude Code / Codex 等）向けの作業規約。

---

## 0. 最初に読むもの

1. `PRODUCT_SPEC.md` — **単一の真実**。§1〜§76 が原仕様、§77 が実装確定事項
2. `TEST_PLAN.md` — テストケースと受け入れ基準の単一の真実
3. `README.md` — 製品の概要と配布方法

矛盾を見つけた場合は、勝手に解釈せず**人間に確認する**。

---

## 1. 役割分担

```text
Claude Code が実装 → Codex がレビュー → Claude Code が修正
```

- Claude Code と Codex に**同時に別実装をさせない**（原仕様§73）
- Codex はレビュワーとして使う。レビュー観点は §7 参照

---

## 2. 絶対に破ってはならない制約

| 禁止事項 | 理由 |
|---|---|
| ユーザー画像を外部サーバーへ送信する | プライバシー（原仕様§52） |
| OpenAI API 等のAI APIを呼び出す | 同上。本製品はAI通信を一切行わない |
| APIキーを要求する | 同上 |
| ログイン・アカウントを要求する | 同上 |
| 実行時にCDNを必須にする | オフライン動作（原仕様§54） |
| LINE仕様の数値をコード中に直接書く | `src/config/line-spec.ts` に一元管理（原仕様§6） |
| `src/core/**` から `src/platform/**` や `src/ui/**` を import する | テスト可能性（§77.3）。`tests/unit/architecture.test.ts` で検証 |
| 通常画面に内部技術用語を表示する | UX原則（原仕様§4） |

**通常画面に出してはならない語：**
Alpha / Connected Components / Bounding Box / Canvas / Flood Fill / Blob / PNG compression / DPI / Cluster / ZIP structure

代わりに「背景を確認しています」「9個のスタンプを見つけました」等の平易な日本語を使う。

---

## 3. コーディング規約

- TypeScript は `strict: true`。`any` を使わない。`as` によるキャストは最小限にし、使う場合は理由をコメントする
- `src/core/**` は純粋関数のみ。副作用・DOM・グローバル状態を持たない
- ファイルが大きくなったら責務が多すぎる兆候。分割する
- コメントは「なぜ」を書く。「何を」はコードで表現する
- ユーザー向け文言は日本語。コード・型・関数名は英語
- 依存パッケージを増やす場合は、事前に理由を提示して承認を得る

### レイヤ依存の方向（一方向のみ）

```text
config  ←  core  ←  platform  ←  state  ←  ui
```

矢印の向きにのみ import してよい。逆方向は禁止。

---

## 4. 変更してよい範囲

- `PRODUCT_SPEC.md` の**原仕様（§1〜§76）は書き換えない**。変更が必要なら §77 に「原仕様からの意図的な変更」として理由付きで追記する
- 大きなアーキテクチャ変更は、理由を説明して承認を得てから行う（原仕様§58）
- 既に動作している機能を、新機能追加のために壊さない

---

## 5. コマンド

```bash
npm install          # 依存インストール
npm run dev          # 開発サーバー
npm run build        # Web版ビルド → dist/
npm run build:offline # オフライン単一HTML → dist-offline/
npm test             # Vitest（単体 + 画像回帰）
npm run test:e2e     # Playwright（E2E）
npm run typecheck    # tsc --noEmit
npm run check        # typecheck + test をまとめて実行
```

**作業完了を報告する前に `npm run check` を実行し、実際の出力を確認すること。**
テストを実行せずに「通りました」と書いてはならない。

---

## 6. フェーズ終了時の報告フォーマット

各フェーズの終わりに、必ず次の5項目で報告する（原仕様§72）。

```markdown
## Phase N 完了報告

### 実装内容
（何を作ったか）

### 変更ファイル
（追加・変更・削除したファイルの一覧）

### テスト結果
（実際に実行したコマンドと、その出力の要約。件数を含める）

### 未解決事項
（既知の不具合、後回しにした判断、確信のない箇所）

### 次の作業
（次フェーズで何をするか）
```

未解決事項が無い場合も「なし」と明記する。省略しない。

---

## 7. Codex レビュー観点

Codex には `PRODUCT_SPEC.md` / `src/**` / `tests/**` を読ませ、次を確認させる（原仕様§73）。

1. **仕様適合** — PRODUCT_SPEC.md の要求を満たしているか
2. **要求漏れ** — 実装されていない仕様項目
3. **画像処理のバグ** — 境界条件、オフバイワン、座標系の取り違え、極端なアスペクト比
4. **UX問題** — 初心者が詰まる箇所、内部用語の露出、エラーメッセージの分かりにくさ
5. **不要な複雑さ** — YAGNI違反、過剰な抽象化
6. **セキュリティ / プライバシー** — 外部送信、CDN依存、意図しないデータ保持
7. **LINE書き出しの正しさ** — 寸法、偶数、カラータイプ、ファイル名、ZIP構成、容量

---

## 8. 迷ったときの判断順序（原仕様§76）

```text
1. 簡単か
2. 失敗しにくいか
3. ユーザーが理解できるか
4. ローカルだけで動くか
5. LINE提出データを正しく作れるか
6. 高機能か
```

高機能であることは最優先ではない。**迷ったら機能を削る。**

---

## 9. コミット

- 1コミット1目的
- メッセージは日本語で可。何を・なぜ を書く
- フェーズ完了時にはタグを打つ（`phase-0`, `phase-1`, ...）
